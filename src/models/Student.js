export default class Student {

    constructor(
        studentId,
        studentName,
        age
    ) {
        this.studentId = studentId;
        this.studentName = studentName;
        this.age = age;
    }
}